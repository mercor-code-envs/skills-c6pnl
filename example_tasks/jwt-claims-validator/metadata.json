{
  "task_name": "jwt-claims-validator",
  "category": "Authentication & Security",
  "input_files": ["auth_spec.txt", "sample_config.json"],
  "test_file": "tests/test.py",
  "solution_file": "solution/solve.sh",
  "golden_skills": ["jwt-claim-validation-order", "jwt-timing-safe-verify"],
  "distractor_skills": ["oauth2-pkce-flow", "session-token-rotation", "api-key-hmac-signing", "password-bcrypt-hashing"],
  "failure_modes": {
    "gemini-3.1-pro-base": "used clock_skew=60s (test requires 5s) causing multiple expiry boundary tests to fail; validated sub with 'not sub or not isinstance(sub, str)' which passes null-byte strings ('user\\x00admin'), failing test_null_byte_in_sub_raises; reward=0.0",
    "claude-opus-4-6-base": "used CLOCK_SKEW=30s instead of required 5s; never validates sub claim at all (no sub check), failing test_missing_sub_raises and test_empty_sub_raises; checks iss/aud only if present in payload so missing-claim tests fail; reward=0.0",
    "gemini-3.1-pro-with-skills": "tried README.md first (not found), then ran find to discover skill files, read jwt-claim-validation-order/SKILL.md and jwt-timing-safe-verify/SKILL.md; used strict validation order, hmac.compare_digest, 5s clock skew, and base64url-without-padding; reward=1.0",
    "claude-opus-4-6-with-skills": "read jwt-claim-validation-order/SKILL.md and jwt-timing-safe-verify/SKILL.md plus both skills' helper scripts; applied strict ordering (format→signature→exp→nbf→iss→aud→sub), hmac.compare_digest for timing-safe comparison, 5s clock skew on exp and nbf, and base64url encoding without padding; reward=1.0"
  }
}
