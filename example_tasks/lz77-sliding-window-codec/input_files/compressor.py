WINDOW_SIZE = 255    # size of the search window
LOOKAHEAD_SIZE = 15  # maximum match length


def encode(data: str) -> bytes:
    """Compress a string using sliding window compression.
    Returns compressed bytes.
    """
    pass


def decode(compressed: bytes) -> str:
    """Decompress compressed bytes back to the original string."""
    pass
