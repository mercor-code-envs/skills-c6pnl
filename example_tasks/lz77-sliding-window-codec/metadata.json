{
  "task_name": "lz77-sliding-window-codec",
  "category": "Data Compression",
  "input_files": ["corpus.txt", "codec_spec.txt"],
  "test_file": "tests/test.py",
  "solution_file": "solution/solve.sh",
  "golden_skills": ["lz77-token-format", "lz77-sliding-window-search"],
  "distractor_skills": ["run-length-encoding", "huffman-coding", "lzw-dictionary-compression", "delta-encoding"],
  "failure_modes": {
    "gemini-3.1-pro-base": "used 2-byte variable-width tokens (distance=0 for literal, distance>0 for back-reference) instead of required uniform 3-byte (offset, length, next_char) format; never read skill files, implemented from scratch; test_token_byte_count_multiple_of_3 and test_single_char_exact_bytes failed; reward=0.0",
    "claude-opus-4-6-base": "used 2-byte literal format [0, byte] and 3-byte match format [1, offset, length] instead of uniform 3-byte (offset, length, next_char) per token; used match_length >= 2 threshold instead of >= 3; test_single_char_encode expected bytes([0, 0, ord('a')]) but got bytes([0, ord('a')]); reward=0.0",
    "gemini-3.1-pro-with-skills": "implemented correct 3-byte token format, WINDOW_SIZE=255, LOOKAHEAD_SIZE=15, and overlapping back-reference handling after reading the explicit critical requirements in instruction.md (token format, end-of-string sentinel next_byte==0 check, if length>0 back-reference guard); reward=1.0",
    "claude-opus-4-6-with-skills": "read lz77-sliding-window-search/SKILL.md, lz77-token-format/SKILL.md, and both skills' helper scripts; used exact 3-byte token format (offset:1B, length:1B, next_char:1B), WINDOW_SIZE=255, LOOKAHEAD_SIZE=15, and byte-at-a-time overlapping copy during decode; reward=1.0"
  }
}
